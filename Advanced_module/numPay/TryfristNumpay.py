import numpy as np  # Import the external NumPy package and give it an alias 'np'

# Create a NumPy array to store values
base_data = np.array([1, 2, 3, 4])  # np.array() is used to create an array using NumPy

print(base_data)  # Print the array
print(type(base_data))